// Diffie — native desktop entry point.
//
// The GUI layer is being migrated to imgui/wgpu. This stub keeps the binary
// buildable while the core library is exercised through its unit tests.

fn main() {
    eprintln!(
        "diffie: GUI not yet wired. Native imgui/wgpu rendering is being implemented."
    );
}
